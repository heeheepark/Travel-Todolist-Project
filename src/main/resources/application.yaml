# db config
spring:
  servlet:
    multipart:
      max-file-size: 3MB
      max-request-size: 12MB

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://${DB_HOST}:3306/team_d
    username: ${DB_USERNAME}
    password: ${DB_USERPASSWORD}

# mybatis config
mybatis:
  type-aliases-package: com.teamd.tt
  mapper-locations: mapper/*.xml

# swagger
springdoc:
  swagger-ui:
    path: /swagger.html
